#include <iostream>
#include <fstream>
#include <cstring>
#include <cmath>

using namespace std;

// It is possible to have auxiliary functions for encoding words into numbers and vice versa.
// For example:
// int encode(char* decoded);
// void decode(int coded, char* decoded);

int main(int argc, char* argv[]) {

    // Insert code here

    return 0;
}
